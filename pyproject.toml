[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "ornl_rust_marimo_portal"
version = "0.1.0"
description = "Marimo Application Launcher GUI built with Rust/egui"
requires-python = ">=3.8"

[tool.maturin]
bindings = "bin"
